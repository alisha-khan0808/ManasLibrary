{
  "name": "manas-library",
  "version": "0.1.0",
  "private": true,
  "description": "Manas Library Management System — multi-branch library operations platform",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "build": "npm run build -w @manas/shared && npm run build -w @manas/api && npm run build -w @manas/web",
    "build:shared": "npm run build -w @manas/shared",
    "dev": "npm-run-all --parallel dev:api dev:web",
    "dev:api": "npm run dev -w @manas/api",
    "dev:web": "npm run dev -w @manas/web",
    "lint": "npm run lint -w @manas/api && npm run lint -w @manas/web",
    "typecheck": "npm run typecheck -w @manas/shared && npm run typecheck -w @manas/api && npm run typecheck -w @manas/web",
    "test": "npm run test -w @manas/api",
    "migrate": "npm run migrate -w @manas/api",
    "seed": "npm run seed -w @manas/api"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^5.5.4"
  }
}
